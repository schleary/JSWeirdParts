Building Objects:

Function Constructors, 'new,' and the history of javascript

A normal function that is used to construct objects

The 'this' variable points to t anew object, and that object is returned from the function automatically